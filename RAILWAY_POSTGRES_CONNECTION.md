# Подключение PostgreSQL к API на Railway

## ✅ Текущий статус:
- PostgreSQL сервис развернут и работает
- Предупреждение о SSH - это нормально, не критично

## 🔗 Как подключить PostgreSQL к API:

### Вариант 1: Через настройки PostgreSQL (рекомендуется)

1. **Откройте PostgreSQL сервис** в Railway
2. Перейдите в **Settings** (шестеренка вверху)
3. Найдите раздел **"Connect Service"** или **"Service Connections"**
4. В списке выберите ваш **API сервис**
5. Нажмите **Connect** или **Add Connection**

Railway автоматически добавит переменную `DATABASE_URL` в ваш API сервис.

### Вариант 2: Через настройки API

1. **Откройте ваш API сервис** в Railway
2. Перейдите в **Settings**
3. Найдите раздел **"Service Connections"** или **"Connected Services"**
4. Нажмите **+ Connect Service** или **Add Service**
5. Выберите **PostgreSQL** из списка
6. Нажмите **Connect**

### Вариант 3: Через переменные окружения (если автоматическое подключение не работает)

1. **Откройте PostgreSQL сервис**
2. Перейдите в **Variables**
3. Найдите переменную `DATABASE_URL` или `POSTGRES_URL`
4. Скопируйте значение
5. **Откройте API сервис**
6. Перейдите в **Variables**
7. Добавьте новую переменную:
   - **Name:** `DATABASE_URL`
   - **Value:** (вставьте скопированное значение)

## 🔍 Как проверить, что подключено:

### В логах API после перезапуска должно быть:
- ✅ `DATABASE_URL: set` (вместо "not set")
- ✅ `Database connection successful`
- ✅ `Migrations applied successfully`

### В настройках API сервиса:
1. Откройте **API сервис** → **Variables**
2. Должна быть переменная `DATABASE_URL`
3. Значение должно начинаться с `postgresql://` или `postgres://`

## ⚠️ Важно:

- **Не добавляйте DATABASE_URL вручную**, если Railway может сделать это автоматически
- **Используйте "Connect Service"** - это правильный способ
- После подключения Railway **автоматически перезапустит** API сервис

## 🐛 Если подключение не работает:

1. **Проверьте, что оба сервиса в одном проекте**
2. **Убедитесь, что PostgreSQL запущен** (Status: Online)
3. **Попробуйте отключить и подключить заново**
4. **Проверьте логи API** - должны быть сообщения о подключении

## 📝 После подключения:

1. Railway автоматически перезапустит API
2. В логах должны появиться сообщения о подключении к БД
3. Миграции применятся автоматически
4. Healthcheck должен пройти успешно

---

**Примечание:** Предупреждение "We are unable to connect to the database via SSH" - это нормально. Railway использует внутренние подключения через переменные окружения, SSH не требуется для работы приложения.

