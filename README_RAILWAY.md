# 🚂 Railway Deployment - Готово к деплою!

Проект полностью настроен для деплоя на Railway.

## ✅ Что было сделано:

1. **Dockerfile** - обновлен для работы с Railway PORT переменной
2. **Program.cs** - автоматически использует Railway DATABASE_URL и REDIS_URL
3. **railway.json** - конфигурация для Railway
4. **.railwayignore** - исключает ненужные файлы из деплоя
5. **appsettings.json** - очищен, использует переменные окружения
6. **appsettings.Development.json** - для локальной разработки

## 🚀 Быстрый старт:

### 1. Подготовка:
```bash
git add .
git commit -m "Ready for Railway deployment"
git push
```

### 2. На Railway:
1. Зайдите на https://railway.app
2. "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Добавьте PostgreSQL и Redis сервисы
5. Настройте переменные окружения (см. RAILWAY_ENV_VARS.md)
6. Подключите сервисы к API
7. Деплой!

## 📋 Подробные инструкции:

- **RAILWAY_DEPLOY_COMPLETE.md** - полная пошаговая инструкция
- **RAILWAY_ENV_VARS.md** - список переменных окружения

## 🔧 Локальная разработка:

Для локальной разработки используйте `compose.yaml`:

```bash
docker compose up -d
```

Приложение будет использовать `appsettings.Development.json` автоматически.

## ✨ Особенности:

- ✅ Автоматическое определение Railway переменных
- ✅ Fallback на appsettings.json для локальной разработки
- ✅ Автоматические миграции при старте
- ✅ Поддержка Railway PORT переменной
- ✅ SSL/TLS для PostgreSQL подключения

## 🎯 Готово к деплою!

Проект полностью готов. Следуйте инструкциям в `RAILWAY_DEPLOY_COMPLETE.md`.

