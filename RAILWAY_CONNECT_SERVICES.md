# Как подключить PostgreSQL к API на Railway

## 🔍 Где найти "Connect Service":

### Вариант 1: В настройках PostgreSQL сервиса

1. **Откройте PostgreSQL сервис** (который вы сейчас смотрите)
2. Прокрутите вниз в настройках
3. Ищите раздел:
   - **"Connected Services"**
   - **"Service Connections"** 
   - **"Connect to Service"**
   - **"Link Service"**
   - Или кнопку **"+ Connect"** / **"Add Connection"**

### Вариант 2: В настройках API сервиса (более вероятно)

1. **Откройте ваш API сервис** (не PostgreSQL!)
2. Перейдите в **Settings**
3. Прокрутите вниз
4. Найдите раздел:
   - **"Service Connections"**
   - **"Connected Services"**
   - **"Dependencies"**
   - Или кнопку **"+ Connect Service"**

### Вариант 3: Через вкладку "Variables"

1. **Откройте ваш API сервис**
2. Перейдите в **Variables**
3. Railway может автоматически показывать доступные сервисы для подключения
4. Или проверьте, есть ли уже переменная `DATABASE_URL` (если есть - уже подключено!)

## 🔗 Альтернативный способ: Через переменные окружения

Если раздел "Connect Service" не найден, можно добавить вручную:

1. **Откройте PostgreSQL сервис**
2. Перейдите в **Variables**
3. Найдите переменную `DATABASE_URL` или `POSTGRES_URL`
4. Если её нет, Railway может использовать формат:
   - `postgresql://postgres:password@postgres.railway.internal:5432/railway`
   - Или через прокси: `postgresql://postgres:password@yamanote.proxy.rlwy.net:48739/railway`

5. **Откройте API сервис**
6. Перейдите в **Variables**
7. Добавьте новую переменную:
   - **Name:** `DATABASE_URL`
   - **Value:** (скопируйте из PostgreSQL или используйте внутренний адрес)

## 📋 Используя внутренний адрес Railway:

Из ваших настроек видно:
- **Внутренний адрес:** `postgres.railway.internal`
- **Порт:** `5432`

Формат DATABASE_URL:
```
postgresql://postgres:PASSWORD@postgres.railway.internal:5432/railway
```

Где `PASSWORD` нужно получить из переменных PostgreSQL сервиса.

## 🔑 Как получить пароль:

1. **Откройте PostgreSQL сервис**
2. Перейдите в **Variables**
3. Найдите переменную:
   - `POSTGRES_PASSWORD`
   - `PGPASSWORD`
   - Или `DATABASE_URL` (там будет полная строка с паролем)

## ✅ Быстрый способ:

1. **Откройте PostgreSQL сервис** → **Variables**
2. Скопируйте значение `DATABASE_URL` (если есть)
3. Если нет, создайте его в формате:
   ```
   postgresql://postgres:ВАШ_ПАРОЛЬ@postgres.railway.internal:5432/railway
   ```
4. **Откройте API сервис** → **Variables**
5. Добавьте переменную `DATABASE_URL` со скопированным значением

## 🎯 После добавления:

1. Railway автоматически перезапустит API
2. В логах должно появиться: `DATABASE_URL: set`
3. Приложение подключится к базе данных

---

**Совет:** Если в интерфейсе Railway есть кнопка "Connect Service" - используйте её, это самый простой способ. Если нет - добавьте `DATABASE_URL` вручную в Variables API сервиса.

