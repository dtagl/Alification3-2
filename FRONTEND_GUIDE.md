# Руководство для фронтенда - Room Booking System

## 🎯 Общая логика работы приложения

### Поток пользователя:
1. **Запуск бота** → проверка существования пользователя
2. **Если нет** → страница присоединения (создать/войти)
3. **Если есть** → главная страница с комнатами
4. **Выбор комнаты** → выбор даты → выбор времени → бронирование
5. **Управление** → мои брони, поиск, админ панель

---

## 🔐 Аутентификация - ЗАЧЕМ и КАК

### При запуске бота (страница Entry)
**ЗАЧЕМ:** Проверить, зарегистрирован ли пользователь в системе

**КАК:**
```
POST /api/first/entrypage
Body: { "telegramId": 123456789 }
```

**ЧТО ДЕЛАТЬ:**
- Если ответ 200 с токеном → пользователь существует → перейти на главную страницу
- Если ответ 404 → пользователя нет → показать страницу "Присоединения"

**КУДА ВЕДЕТ:**
- ✅ Есть токен → `/home` (главная страница)
- ❌ Нет токена → `/join` (страница присоединения)

---

### Страница "Присоединения" (`/join`)
**ЗАЧЕМ:** Пользователь может создать новую компанию или войти в существующую

**ДВЕ КНОПКИ:**

#### 1. "Создать" → `/create-company`
**ЗАЧЕМ:** Создать новую компанию и стать её админом

**КАК:**
```
POST /api/first/create-company
Body: {
  "companyName": "Моя компания",
  "password": "secret123",
  "telegramId": 123456789,
  "userName": "Иван",
  "workingStart": "09:00:00",  // Начало рабочего дня
  "workingEnd": "18:00:00"     // Конец рабочего дня
}
```

**ЧТО ПРОИСХОДИТ:**
1. Создается компания в БД
2. Пользователь становится Admin этой компании
3. Возвращается JWT токен
4. Сохранить токен в localStorage
5. Перейти на `/home`

**КУДА ВЕДЕТ:** `/home` (главная страница)

---

#### 2. "Войти" → `/login`
**ЗАЧЕМ:** Войти в существующую компанию

**КАК:**
```
POST /api/first/login-telegram
Body: {
  "telegramId": 123456789,
  "companyName": "Моя компания",  // Название компании
  "companyPassword": "secret123",  // Пароль компании (обязательно для новых пользователей)
  "userName": "Иван" (optional)
}
```

**ЧТО ПРОИСХОДИТ:**
1. Если пользователь существует → возвращается токен (companyName и companyPassword не требуются)
2. Если пользователя нет → требуется companyName и companyPassword для проверки доступа
3. Пароль проверяется перед созданием нового пользователя
4. Сохранить токен в localStorage
5. Перейти на `/home`

**КУДА ВЕДЕТ:** `/home` (главная страница)

---

## 🏠 Главная страница (`/home`) - ЗАЧЕМ и КАК

**ЗАЧЕМ:** Показать все комнаты компании и навигацию

**КАК получить список комнат:**
```
GET /api/rooms/company
Headers: Authorization: Bearer {token}
```

**ЧТО ПОКАЗАТЬ:**
1. **Кнопки комнат** (динамически из ответа)
   - Каждая кнопка = одна комната
   - При клике → `/room/{roomId}`

2. **Кнопка "Мои брони"** → `/my-bookings`
   - Показывает все брони пользователя

3. **Кнопка "Доступные сейчас"** → `/available-now`
   - Показывает комнаты, свободные прямо сейчас

4. **Кнопка "Поиск"** → `/search`
   - Поиск комнат по фильтрам

5. **Кнопка "Админ"** → `/admin` (только если роль = Admin)
   - Панель управления для администратора

**КУДА ВЕДЕТ:**
- Комната → `/room/{roomId}`
- Мои брони → `/my-bookings`
- Доступные сейчас → `/available-now`
- Поиск → `/search`
- Админ → `/admin`

---

## 📅 Страница комнаты (`/room/{roomId}`) - ЗАЧЕМ и КАК

**ЗАЧЕМ:** Показать доступные даты и времена для бронирования

### Шаг 1: Выбор даты
**КАК:** Фронтенд сам создает список дат
- Начать с сегодняшней даты
- Показать следующие 7-14 дней
- Каждая дата = кнопка

**КУДА ВЕДЕТ:** При клике на дату → показать таймслоты для этой даты

---

### Шаг 2: Выбор времени (таймслоты)
**КАК получить таймслоты:**
```
GET /api/rooms/{roomId}/timeslots?date=2024-11-07T00:00:00Z
Headers: Authorization: Bearer {token}
```

**ЧТО ВЕРНЕТСЯ:**
```json
{
  "2024-11-07T09:00:00Z": true,   // true = свободно
  "2024-11-07T09:15:00Z": true,
  "2024-11-07T09:30:00Z": false,  // false = занято
  "2024-11-07T09:45:00Z": true,
  "2024-11-07T10:00:00Z": true,
  ...
}
```

**КАК ОТОБРАЗИТЬ:**
1. **Группировать по часам:**
   - 09:00-09:45 (4 слота по 15 минут)
   - 10:00-10:45
   - 11:00-11:45
   - и т.д.

2. **Цветовая индикация:**
   - 🟢 **Зеленый** (`true`) = свободно → показать кнопку "Забронировать"
   - 🔴 **Красный** (`false`) = занято → НЕ показывать кнопку "Забронировать"

3. **При клике на таймслот:**
   - Показать внизу информацию о бронировании
   - Вызвать `/api/rooms/{roomId}/booking-info?time={DateTime}`

---

### Получение информации о бронировании
**ЗАЧЕМ:** Показать, кто забронировал это время (или что свободно)

**КАК:**
```
GET /api/rooms/{roomId}/booking-info?time=2024-11-07T09:30:00Z
Headers: Authorization: Bearer {token}
```

**ЧТО ВЕРНЕТСЯ:**
```json
{
  "userName": "Иван" | null,  // null если свободно
  "startAt": "2024-11-07T09:00:00Z",
  "endAt": "2024-11-07T10:00:00Z",
  "isBooked": true
}
```

**ЛОГИКА ОТОБРАЖЕНИЯ:**
- Если `userName === null` → показать "Свободно"
- Если `userName` есть:
  - Показать "Забронировано: {userName}"
  - Если `userName === текущий пользователь` ИЛИ `роль === Admin`:
    - Показать кнопку "Удалить бронь"

---

### Бронирование комнаты
**ЗАЧЕМ:** Забронировать выбранное время

**КАК:**
```
POST /api/rooms/{roomId}/book
Headers: Authorization: Bearer {token}
Body: {
  "startAt": "2024-11-07T09:00:00Z",
  "endAt": "2024-11-07T09:15:00Z"  // Можно выбрать 15 мин, 30 мин, 1 час и т.д.
}
```

**ЧТО ДЕЛАТЬ:**
- Если успешно (200) → показать "Бронь создана", обновить список таймслотов
- Если конфликт (409) → показать "Время уже занято"
- Если ошибка (400) → показать сообщение об ошибке

**КУДА ВЕДЕТ:** Остаемся на странице комнаты, обновляем таймслоты

---

### Удаление брони
**ЗАЧЕМ:** Отменить свою бронь (или админ может удалить любую)

**КАК:**
```
DELETE /api/rooms/booking/{bookingId}
Headers: Authorization: Bearer {token}
```

**ВАЖНО:** `userId` автоматически берется из JWT токена, не нужно передавать в запросе!

**КОГДА ПОКАЗЫВАТЬ КНОПКУ:**
- Если бронь принадлежит текущему пользователю
- ИЛИ если роль пользователя = Admin

**ЧТО ДЕЛАТЬ:**
- Если успешно (200) → показать "Бронь удалена", обновить список таймслотов
- Если ошибка (403) → показать "Нет прав на удаление"

---

## 📋 Мои брони (`/my-bookings`) - ЗАЧЕМ и КАК

**ЗАЧЕМ:** Показать все брони пользователя (активные и прошедшие)

**КАК:**
```
GET /api/home/my-bookings
Headers: Authorization: Bearer {token}
```

**ЧТО ВЕРНЕТСЯ:**
```json
{
  "active": [
    {
      "id": Guid,
      "roomId": Guid,
      "roomName": "Комната 1",
      "startAt": DateTime,
      "endAt": DateTime
    }
  ],
  "past": [
    // те же объекты, но с EndAt < сейчас
  ]
}
```

**КАК ОТОБРАЗИТЬ:**
1. **Активные брони** (EndAt >= сейчас)
   - Показать список сверху
   - Для каждой: комната, время, кнопка "Отменить"

2. **Разделительная линия**

3. **Прошедшие брони** (EndAt < сейчас)
   - Показать список снизу
   - Только для просмотра (без кнопки отмены)

**КУДА ВЕДЕТ:** При клике на бронь → `/room/{roomId}` с выбранной датой

---

## 🔍 Доступные сейчас (`/available-now`) - ЗАЧЕМ и КАК

**ЗАЧЕМ:** Показать комнаты, которые свободны прямо в этот момент

**КАК:**
```
GET /api/home/available-now
Headers: Authorization: Bearer {token}
```

**ЧТО ВЕРНЕТСЯ:**
```json
[
  {
    "id": Guid,
    "name": "Комната 1",
    "capacity": 10,
    "description": "Большая комната"
  }
]
```

**КАК ОТОБРАЗИТЬ:**
- Список комнат
- Каждая комната = кнопка
- При клике → `/room/{roomId}`

**КУДА ВЕДЕТ:** `/room/{roomId}`

---

## 🔎 Поиск комнат (`/search`) - ЗАЧЕМ и КАК

**ЗАЧЕМ:** Найти комнаты по фильтрам (вместимость, название, время и т.д.)

**КАК:**
```
GET /api/rooms/findroom?MinCapacity=5&MaxCapacity=10&Name=Conference&StartAt=2024-11-07T14:00:00Z&EndAt=2024-11-07T15:00:00Z
Headers: Authorization: Bearer {token}
```

**ФИЛЬТРЫ (все опциональны):**
- `MinCapacity`: минимальная вместимость
- `MaxCapacity`: максимальная вместимость
- `Name`: поиск по названию (частичное совпадение)
- `Description`: поиск по описанию (частичное совпадение)
- `StartAt`: начало времени для проверки доступности
- `EndAt`: конец времени для проверки доступности

**ЧТО ВЕРНЕТСЯ:**
```json
[
  {
    "id": Guid,
    "name": "Комната 1",
    "capacity": 10,
    "description": "...",
    "isAvailable": true  // доступна ли в указанное время
  }
]
```

**КАК ОТОБРАЗИТЬ:**
- Форма с фильтрами (все опциональны)
- Кнопка "Найти"
- Список результатов
- Для каждой комнаты: если `isAvailable === true` → кнопка "Забронировать"

**КУДА ВЕДЕТ:** При клике на комнату → `/room/{roomId}`

---

## 👑 Админ панель (`/admin`) - ЗАЧЕМ и КАК

**ЗАЧЕМ:** Управление компанией (только для Admin)

**ДОСТУП:** Только если `role === "Admin"` (из JWT токена)

### Обзор компании
**ЗАЧЕМ:** Показать статистику компании

**КАК:**
```
GET /api/admin/overview
Headers: Authorization: Bearer {token}
```

**ЧТО ВЕРНЕТСЯ:**
```json
{
  "totalRooms": 5,
  "totalUsers": 20,
  "totalBookings": 150,
  "activeBookings": 10
}
```

---

### Создать комнату
**ЗАЧЕМ:** Добавить новую комнату в компанию

**КАК:**
```
POST /api/rooms/create
Headers: Authorization: Bearer {token}
Body: {
  "name": "Комната 5",
  "capacity": 15,
  "description": "Новая комната"
}
```

**ВАЖНО:** `companyId` автоматически берется из JWT токена, не нужно передавать в запросе!

**ЧТО ДЕЛАТЬ:**
- Показать форму создания
- После успеха → обновить список комнат на главной странице

---

### Утилизация комнат
**ЗАЧЕМ:** Показать процент использования каждой комнаты

**КАК:**
```
GET /api/admin/room-utilization
Headers: Authorization: Bearer {token}
```

**ЧТО ВЕРНЕТСЯ:**
```json
[
  { "room": "Комната 1", "utilizationPercent": 75.5 },
  { "room": "Комната 2", "utilizationPercent": 45.2 }
]
```

**КАК ОТОБРАЗИТЬ:** График или таблица с процентами

---

### Топ комнат
**ЗАЧЕМ:** Показать самые популярные комнаты

**КАК:**
```
GET /api/admin/top-rooms
Headers: Authorization: Bearer {token}
```

**ЧТО ВЕРНЕТСЯ:**
```json
[
  { "room": "Комната 1", "count": 50 },
  { "room": "Комната 2", "count": 30 }
]
```

---

### Активность пользователей
**ЗАЧЕМ:** Показать статистику по пользователям

**КАК:**
```
GET /api/admin/user-activity
Headers: Authorization: Bearer {token}
```

**ЧТО ВЕРНЕТСЯ:**
```json
[
  {
    "user": "Иван",
    "bookings": 25,
    "totalHours": 50.5
  }
]
```

---

### Тренды бронирований
**ЗАЧЕМ:** Показать динамику бронирований за последние 7 дней

**КАК:**
```
GET /api/admin/bookings-trend
Headers: Authorization: Bearer {token}
```

**ЧТО ВЕРНЕТСЯ:**
```json
[
  { "date": "2024-11-01", "count": 10 },
  { "date": "2024-11-02", "count": 15 }
]
```

**КАК ОТОБРАЗИТЬ:** График по дням

---

### Управление пользователями

#### Все пользователи
```
GET /api/admin/all-users
Response: [
  { "id": Guid, "userName": "Иван", "role": "Admin" | "User" }
]
```

#### Сделать админом
```
PUT /api/admin/make-admin/{userId}
```

#### Убрать админа
```
PUT /api/admin/revoke-admin/{userId}
```

#### Удалить пользователя
```
DELETE /api/admin/delete-user/{userId}
```

---

### Настройки компании

#### Изменить рабочее время
```
PUT /api/admin/change-working-hours
Body: {
  "workingStart": "08:00:00",
  "workingEnd": "20:00:00"
}
```

#### Изменить название компании
```
PUT /api/admin/change-company-name
Body: "Новое название"  // просто строка
```

#### Изменить пароль компании
```
PUT /api/admin/change-password
Body: "новый_пароль"  // просто строка
```

---

## 🔑 JWT Token - КАК РАБОТАТЬ

### Хранение
**ГДЕ:** `localStorage.setItem('token', token)` или `sessionStorage`

### Использование
**В КАЖДОМ ЗАПРОСЕ:**
```
Headers: {
  "Authorization": "Bearer {token}"
}
```

### Извлечение данных из токена
**КАК:** Декодировать JWT (использовать библиотеку типа `jwt-decode`)

**ЧТО МОЖНО ПОЛУЧИТЬ:**
- `sub` → `userId` (Guid)
- `name` → `userName` (string)
- `role` → `"Admin"` или `"User"`
- `companyId` → `companyId` (Guid) - custom claim

**ЗАЧЕМ:**
- Проверять роль для показа/скрытия админ панели
- Проверять, может ли пользователь удалить бронь
- `companyId` и `userId` автоматически используются в запросах (не нужно передавать вручную)

---

## 📱 Структура страниц - КУДА ЧТО ВЕДЕТ

```
/entry → проверка пользователя
  ├─ есть → /home
  └─ нет → /join

/join → присоединение
  ├─ Создать → /create-company → /home
  └─ Войти → /login → /home

/home → главная страница
  ├─ Комната → /room/{roomId}
  ├─ Мои брони → /my-bookings
  ├─ Доступные сейчас → /available-now
  ├─ Поиск → /search
  └─ Админ → /admin (только Admin)

/room/{roomId} → страница комнаты
  ├─ Выбор даты → показать таймслоты
  ├─ Выбор времени → бронирование
  └─ Информация о брони → показать внизу

/my-bookings → список броней
  └─ Клик на бронь → /room/{roomId}

/available-now → доступные комнаты
  └─ Клик на комнату → /room/{roomId}

/search → поиск комнат
  └─ Клик на комнату → /room/{roomId}

/admin → админ панель
  └─ Различные функции управления
```

---

## ⚠️ Важные правила

### Авторизация
- **ВСЕ запросы** (кроме `/entrypage`, `/create-company`, `/login-telegram`) требуют JWT токен
- Если токен истек (401) → вернуться на `/entry`

### Роли
- **Admin** → доступ ко всем функциям, может удалять любые брони
- **User** → может только бронировать и удалять свои брони

### Даты и время
- **Формат:** ISO 8601 UTC (`2024-11-07T09:00:00Z`)
- **Таймслоты:** интервалы по 15 минут
- **Группировка:** показывать группами по часу (09:00-09:45, 10:00-10:45)

### Валидация
- **Перед бронированием:** проверить `isBooked === true` (свободно)
- **Перед удалением:** проверить `userName === текущий пользователь` ИЛИ `role === Admin`

### Ошибки
- **401 Unauthorized** → токен истек/неверный → `/entry`
- **403 Forbidden** → нет прав → показать сообщение
- **404 Not Found** → не найдено → показать сообщение
- **409 Conflict** → конфликт (время занято) → показать сообщение
