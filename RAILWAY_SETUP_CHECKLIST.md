# Railway Setup Checklist - Что нужно сделать

## ✅ Хорошие новости:
Приложение **запустилось успешно**! Оно слушает на порту 8080 и готово отвечать на запросы.

## ⚠️ Что нужно настроить:

### 1. Подключить PostgreSQL к API сервису

**Проблема:** `DATABASE_URL: not set`

**Решение:**
1. Откройте ваш проект на Railway
2. Выберите **PostgreSQL сервис**
3. Перейдите в **Settings** → **Connect Service**
4. Выберите ваш **API сервис**
5. Railway автоматически добавит переменную `DATABASE_URL`

### 2. Подключить Redis к API сервису

**Проблема:** `REDIS_URL: not set`

**Решение:**
1. Откройте ваш проект на Railway
2. Выберите **Redis сервис**
3. Перейдите в **Settings** → **Connect Service**
4. Выберите ваш **API сервис**
5. Railway автоматически добавит переменную `REDIS_URL`

### 3. Установить переменные окружения для API

В настройках вашего **API сервиса** добавьте:

#### Telegram Bot Token:
```env
Telegram__BotToken=8394190016:AAFcwClcmcVDs7d6xIPKdP_BJyyj-66uiyc
```

#### JWT Configuration:
```env
Jwt__Issuer=Alification3
Jwt__Audience=Alification3
Jwt__Key=wA3X9p5Z6eN4dG7jKlP9xQ1a8vH2b3zR5yY6tU9oM0cP1nD2hF3sG4rH5tJ6kL7=
Jwt__ExpiryMinutes=120
```

## 📋 Пошаговая инструкция:

### Шаг 1: Подключить PostgreSQL
1. В Railway проекте найдите PostgreSQL сервис
2. Нажмите на него
3. Перейдите в **Settings** (шестеренка)
4. Найдите раздел **"Connect Service"** или **"Service Connections"**
5. Выберите ваш API сервис из списка
6. Нажмите **Connect**

### Шаг 2: Подключить Redis
1. В Railway проекте найдите Redis сервис
2. Нажмите на него
3. Перейдите в **Settings**
4. Найдите раздел **"Connect Service"**
5. Выберите ваш API сервис
6. Нажмите **Connect**

### Шаг 3: Добавить переменные окружения
1. В Railway проекте найдите ваш **API сервис**
2. Нажмите на него
3. Перейдите в **Variables** (или **Settings** → **Variables**)
4. Нажмите **+ New Variable**
5. Добавьте каждую переменную из списка выше

### Шаг 4: Перезапустить API
1. После добавления переменных и подключения сервисов
2. Railway автоматически перезапустит API
3. Или нажмите **Deploy** вручную

## ✅ Проверка после настройки:

После перезапуска в логах должны быть:
- ✅ `DATABASE_URL: set` (вместо "not set")
- ✅ `REDIS_URL: set` (вместо "not set")
- ✅ Нет предупреждения "JWT Key is not configured"
- ✅ Сообщение "Database connection successful"
- ✅ Сообщение "Migrations applied successfully"

## 🎯 Healthcheck должен пройти!

После подключения сервисов и установки переменных:
- Healthcheck endpoint `/api/health` будет работать
- Приложение будет полностью функциональным

## 📝 Важно:

- **Не добавляйте DATABASE_URL и REDIS_URL вручную** - Railway делает это автоматически при подключении сервисов
- **Используйте двойное подчеркивание** (`__`) для вложенных настроек (например, `Telegram__BotToken`)
- **После изменений Railway автоматически перезапустит сервис**

Готово! После выполнения этих шагов приложение будет полностью настроено! 🚀

