# Полная инструкция по деплою на Railway

## ✅ Что уже настроено:

1. ✅ `Dockerfile` - обновлен для работы с Railway PORT
2. ✅ `Program.cs` - поддерживает Railway DATABASE_URL и REDIS_URL
3. ✅ `railway.json` - конфигурация для Railway
4. ✅ `.railwayignore` - исключает ненужные файлы
5. ✅ `appsettings.json` - очищен для использования переменных окружения

## 🚀 Шаги деплоя:

### Шаг 1: Подготовка GitHub репозитория

1. Убедитесь, что все изменения закоммичены:
   ```bash
   git add .
   git commit -m "Prepare for Railway deployment"
   git push
   ```

### Шаг 2: Создание проекта на Railway

1. Перейдите на https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий `Alification3`
6. Railway автоматически обнаружит `railway.json` и `Dockerfile`

### Шаг 3: Добавление PostgreSQL

1. В проекте нажмите "+ New"
2. Выберите "Database" → "PostgreSQL"
3. Railway создаст базу автоматически
4. Скопируйте `DATABASE_URL` из переменных окружения (понадобится позже)

### Шаг 4: Добавление Redis

1. Нажмите "+ New"
2. Выберите "Database" → "Redis"
3. Railway создаст Redis автоматически
4. Скопируйте `REDIS_URL` из переменных окружения

### Шаг 5: Настройка переменных окружения

В настройках вашего **API сервиса** (не базы данных!), добавьте переменные:

#### Обязательные:

```env
ASPNETCORE_ENVIRONMENT=Production
DOTNET_RUNNING_IN_CONTAINER=true
```

#### Telegram:

```env
Telegram__BotToken=8394190016:AAFcwClcmcVDs7d6xIPKdP_BJyyj-66uiyc
```

#### JWT:

```env
Jwt__Issuer=Alification3
Jwt__Audience=Alification3
Jwt__Key=wA3X9p5Z6eN4dG7jKlP9xQ1a8vH2b3zR5yY6tU9oM0cP1nD2hF3sG4rH5tJ6kL7=
Jwt__ExpiryMinutes=120
```

**Важно**: Railway автоматически предоставит `DATABASE_URL` и `REDIS_URL` - не нужно добавлять их вручную!

### Шаг 6: Подключение сервисов

1. В настройках API сервиса найдите "Settings"
2. В разделе "Service" найдите "Connect Service"
3. Подключите PostgreSQL и Redis
4. Railway автоматически добавит переменные `DATABASE_URL` и `REDIS_URL`

### Шаг 7: Деплой

1. Railway автоматически начнет сборку при подключении репозитория
2. Или нажмите "Deploy" вручную
3. Дождитесь завершения сборки (обычно 2-5 минут)
4. Проверьте логи на наличие ошибок

### Шаг 8: Получение URL

1. После успешного деплоя Railway предоставит URL
2. Нажмите на ваш API сервис
3. В разделе "Settings" → "Networking" найдите "Public Domain"
4. Скопируйте URL (например: `https://your-app-name.up.railway.app`)

### Шаг 9: Проверка работы

1. Откройте в браузере: `https://your-app-name.up.railway.app/swagger/index.html`
2. Должен открыться Swagger UI
3. Проверьте логи Railway на наличие ошибок подключения к БД

### Шаг 10: Обновление Telegram Bot

1. Откройте Telegram → @BotFather
2. Отправьте `/setmenubutton`
3. Выберите вашего бота: `@bronkomat_bot`
4. Укажите URL: `https://your-app-name.up.railway.app`
5. Готово! ✅

## 🔍 Troubleshooting

### Ошибка подключения к базе данных:

**Проблема**: `Unable to connect to database`

**Решение**:
1. Убедитесь, что PostgreSQL сервис запущен
2. Проверьте, что API сервис подключен к PostgreSQL (Settings → Connect Service)
3. Проверьте логи Railway - должны быть сообщения о подключении

### Ошибка подключения к Redis:

**Проблема**: Redis не работает

**Решение**:
1. Убедитесь, что Redis сервис запущен
2. Проверьте подключение (Settings → Connect Service)
3. Приложение будет использовать MemoryCache если Redis недоступен

### Порт не работает:

**Проблема**: Приложение не отвечает

**Решение**:
1. Railway автоматически назначает PORT
2. Убедитесь, что `ASPNETCORE_URLS` использует переменную PORT
3. Проверьте логи Railway

### Миграции не применяются:

**Проблема**: База данных не обновляется

**Решение**:
1. Миграции применяются автоматически при старте (см. Program.cs)
2. Проверьте логи Railway на наличие ошибок миграций
3. Убедитесь, что база данных пустая или совместима

## 📊 Мониторинг

Railway предоставляет:
- ✅ Логи в реальном времени
- ✅ Метрики использования ресурсов
- ✅ История деплоев
- ✅ Автоматические бэкапы базы данных

## 💰 Стоимость

Railway предоставляет:
- $5 бесплатных кредитов в месяц
- Обычно достаточно для небольшого приложения
- Платите только за использование сверх лимита

## 🎉 Готово!

Ваше приложение теперь работает на Railway с:
- ✅ Автоматическим HTTPS
- ✅ Управляемыми базами данных
- ✅ Автоматическими обновлениями
- ✅ Мониторингом и логами

**URL вашего приложения**: `https://your-app-name.up.railway.app`

